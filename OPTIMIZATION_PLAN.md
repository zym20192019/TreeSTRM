# TreeSTRM 媒体治理与对账平台 · 架构演进与优化规划 (RFC)

## 一、 项目背景与现状审视

TreeSTRM（运行于 `/opt/treestrm`，端口 `:8028`）在当前的生产运行中已经取得了突破性成果：
- 支撑了 18.9 万+ STRM 资产的 1:1 纯净映射生成与对账；
- 成人专区（FC2、欧美商业 Scene、中字等）实现了纯正原画海报与真实 NFO 100% 全覆盖；
- 剥离了脆弱的 LLM 语义盲猜，全面贯彻确定性 4 级原生瀑布流刮削（官方直取 -> 历史镜像 -> 番号库 -> CD2 原片黄金位抽帧兜底）；
- 严格践行单视频单封面、海报与 NFO 解耦、厂牌硬约束等核心治理准则。

从更高维度、更大体量（几十万级资产常态化流转）及长期免维护的角度审视，当前工程结构仍有明显的演进与优化空间。

---

## 二、 核心瓶颈与优化空间分析

### 1. 代码架构单一膨胀 (Codebase Decoupling)
- **现状**：`app.py` 已经累积超过 1400 行代码，杂糅了 FastAPI 路由定义、115 API 交互、CD2 抽帧、后台 Worker 线程、NFO 生成模板与全局内存状态字典；`scraper_service.py` 承载了 24 站爬虫逻辑。
- **隐患**：局部功能修改或增加新站点容易牵一发动全身，单元测试与排障定位成本高。

### 2. 状态机与任务队列持久化 (State & Queue Reliability)
- **现状**：当前治理任务队列和执行进度保存在 `governance_state.json` 与内存全局变量中（如 `sync_progress`, `gov_progress`）。
- **隐患**：服务重启、异常崩溃或主机调度时，若正在执行深层任务，执行游标容易丢失，需要重新全量比对，缺乏真正的微步事务级断点续跑机制。

### 3. 18万+ 海量资产的高频全盘 I/O 开销 (Reconciliation Performance)
- **现状**：每次触发全量对账时，`os.walk('/Movies/TreeStrms')` 需要遍历十多万个本地文件与目录。
- **隐患**：虽然当前能在数秒到数十秒内完成，但随着媒体库膨胀，全盘 FUSE/磁盘扫描对系统 I/O 和内存造成峰值压力。

### 4. 4K/60fps 高码率原片抽帧瞬时 CPU 峰值 (Resource Governance)
- **现状**：遇到 4K 60fps、高码率 HEVC 原片抽帧时，`ffmpeg` 软件解码会瞬间吃满多核心 CPU（Load Average 飙升），虽属正常物理转码，但短时间内会挤占其他服务算力。
- **隐患**：大批量连续抽帧时容易导致系统感知卡顿。

### 5. 可观测性与 Emby 闭环联动 (Observability & Emby Integration)
- **现状**：前端通过高频轮询 `status` 接口获取日志；刮削对账完成后，需要手动刷新 Emby 或等待 Emby 计划任务。
- **优化点**：缺少刮削落地后向 Emby API 精准触发指定 Item/路径刷新的即时闭环。

---

## 三、 五大核心演进模块设计

### 模块 1：分层解耦与模块化重构

将单一的大文件按职责拆解为清晰的模块层级：

```text
/opt/treestrm/
├── api/                   # API 控制器层（纯路由）
│   ├── tasks.py           # 任务与对账 API
│   ├── categories.py      # 子专区治理工坊 API
│   └── settings.py        # 系统配置与流控 API
├── core/                  # 核心基础层
│   ├── config.py          # 配置加载、校验与热更新
│   ├── events.py          # 日志总线与广播
│   └── state.py           # 运行时状态管理
├── engine/                # 业务引擎层
│   ├── reconciler.py      # 115 目录树流式解析与 1:1 STRM 差异对账
│   ├── cd2_extractor.py   # CD2 FastSeek 黄金位抽帧器（带 CPU 限速与优先级降权）
│   ├── nfo_builder.py     # 真实数据驱动的标准 NFO 构建器
│   └── cleaner.py         # 孤立失效资产安全排查
├── services/              # 外部集成服务
│   ├── scraper/           # Amane 24 站瀑布流与 ThePornDB 深度打捞
│   ├── cloud115.py        # 115 官方 API、目录树导出与 300项/批 batch_rename
│   └── emby_client.py     # Emby 增量局部刷新联动客户端
├── static/                # 前端 Web 资产
├── app.py                 # 极简服务入口 (FastAPI App 初始化与生命周期管理)
```

---

### 模块 2：基于 SQLite WAL 的微步任务状态仓 (Checkpointing)

**设计目标**：实现毫秒级断点续跑与任务可溯源。

1. **数据表设计**：
   - `tasks`：记录每次对账或刮削的全局任务信息（任务ID、类型、目标专区、状态、开始/结束时间）；
   - `task_items`：记录条目级状态（视频路径、BaseName、当前步骤：`DISCOVERED` / `STRM_OK` / `NFO_OK` / `POSTER_OK` / `SKIPPED` / `FAILED`、最后错误信息）。
2. **断点恢复策略**：
   - 调度器启动时自动检查是否存在 `RUNNING` 状态的任务；
   - 自动跳过 `task_items` 中已标记 `COMPLETED` 的条目，直接从中断处恢复执行；
   - 彻底避免由于外部断网或服务升级导致的重复处理。

---

### 模块 3：抽帧引擎算力治理与极速关键帧寻道 (CPU 防爆)

**设计目标**：抽帧过程静默平稳，CPU 峰值可控，单视频抽帧耗时缩短 70% 以上。

1. **ffmpeg 参数优化（极速关键帧寻道）**：
   - 将 `-ss` 参数强制置于 `-i` 之前，启用容器层关键帧快速寻道，禁止全片软解：
     ```bash
     ffmpeg -ss 00:05:00 -noaccurate_seek -i "原片路径" -frames:v 1 -q:v 2 -y "海报路径"
     ```
2. **进程调度优先级降权**：
   - 抽帧子进程增加 `nice -n 15` 和 `ionice -c 3`，确保在 CPU/IO 紧张时主动让位给 Emby 与系统核心服务。
3. **并发自适应限制**：
   - 严格控制抽帧 worker 数量（默认限制为 1~2 线程），杜绝多核同时软解 4K 导致算力过载。

---

### 模块 4：海量资产增量指纹加速 (Fast Fingerprinting)

**设计目标**：将 18 万资产的扫描与差异计算时间压至极致。

1. **子专区快照指纹机制**：
   - 记录各子专区的目录文件数与云端导出文件总数的对比哈希；
   - 对账时，若某个子专区（如「我爱的AV」）文件数与特征无变更，直接跳过深层递归，仅针对发生变更的子专区进行增量操作。
2. **流式目录树生成器**：
   - 115 目录树解析由单次全量加载改造为流式 Generator，边读行边分流，避免突发内存占用。

---

### 模块 5：Emby 闭环联动与前端实时通信

**设计目标**：打通媒体治理的最后一公里，实现全自动无人值守闭环。

1. **Emby 局部精准刷新**：
   - 刮削完成或补齐海报后，TreeSTRM 通过 Emby 本地 API（`:8096`）根据生成的 STRM 路径精确定位媒体项，只刷新该 Item 或子文件夹元数据，严禁触发全局重扫。
2. **WebSocket 双向日志与进度**：
   - 前端从每秒 HTTP 轮询升级为 WebSocket 长连接，实时回传任务流水日志与双进度条百分比，网络开销近乎为零。

---

## 四、 分步实施路线图 (Implementation Roadmap)

| 阶段 | 重点任务 | 预期收益 | 风险与对策 |
| :--- | :--- | :--- | :--- |
| **第一阶段 (Phase 1)<br>抽帧优化与算力防护** | 1. 改造 `extract_cover_by_cd2`，加入关键帧前置寻道<br>2. 增加进程 nice 降权与单 worker 限制 | 彻底解决 4K 60fps 抽帧导致的 CPU 瞬间打满问题，抽帧速度提升 3-5 倍 | 极低风险，纯 ffmpeg 参数优化 |
| **第二阶段 (Phase 2)<br>断点续跑与微状态机** | 1. 建立 `treestrm.db` SQLite 数据表<br>2. 任务队列接入游标存储与重启自动续跑 | 服务重启或异常后无感自动恢复，再也不用手动重头挂任务 | 兼容已有 `governance_state.json`，平滑迁移 |
| **第三阶段 (Phase 3)<br>模块化解耦与重构** | 1. 拆分 `app.py` 为 `core/`、`engine/`、`services/`、`api/`<br>2. 保持对外 API 接口完全兼容，前端无需任何改动 | 代码结构清爽整洁，提升长期可维护性 | 本地充分进行单元回归测试后再推送到 git |
| **第四阶段 (Phase 4)<br>Emby 精准联动与增量对账** | 1. 引入目录级增量指纹跳过机制<br>2. 任务完成自动触发 Emby 路径级增量刷新 | 实现从网盘 -> 生成 -> 刮削 -> Emby 上架的完整无人值守闭环 | 严格限制 Emby 只调用局部刷新 API，杜绝全库刷新 |

---

## 五、 质量准则与铁律（对齐既有规范）

在推进任何重构与优化时，必须严格遵守既定铁律：
1. **STRM 1:1 原名原则**：统一采用纯净 `basename.strm`，坚决不带任何中间媒体格式后缀；
2. **真实官方数据优先**：NFO 必须由网络真实检索结果写入，无官方数据坚决不生造假 NFO；
3. **单视频单封面**：每部影片严格对应 1 张合规静态海报，拒绝留白，抽帧仅作为官方无图的兜底保障；
4. **厂牌硬约束**：严格校对厂牌与演员对应关系，宁缺毋滥，严禁跨厂牌乱配；
5. **115 流控安全**：单 Cookie 请求间隙保持 `>= 1.0s~2.0s`，批量更名严格遵循 `300项/批` 官方极速通道。
